//define an array of the icons

//set interval of 1 sec to replace inner html

//loop through the array and choose next item\

// if (item[i+1]) {iconDiv.insertInnerHtml(item[i+1])
// } else {
//     iconDiv
// }
//when array ends go back to one
